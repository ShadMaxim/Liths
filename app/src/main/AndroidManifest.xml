<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.serenitysoul">

    <application
        android:name="com.example.serenitysoul.activity.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_app"
        android:label="${appLabel}"
        android:roundIcon="@mipmap/ic_launcher_app_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name="com.example.serenitysoul.activity.MainActivity"

            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>

    <!--android:icon="${appIcon}"-->
    <!--android:icon="@mipmap/ic_launcher_app"-->
<!--    android:theme="@style/SplashLauncherTheme"-->